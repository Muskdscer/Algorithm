package com.example.algorithm2025.leetcode01.twopointer;

import java.math.BigDecimal;

public class DiDiTest {
    private serviceB;
    private serviceA;

    public static void main(String[] args) {
        String count
        if (addCount(count)) {
            String localCount = serviceA.getCountById(Long id);
            String newCount = new BigDecimal(count).subtract(new BigDecimal(count)).toString();
            boolean flagA = serviceA.deductCountnewCount);
            if (flagA) {

            } else {

            }
        } else {
            serviceA.backCount(count);
        }
    }

    private boolean addCount() {
        boolean flag = serviceB.add(String count);
        update b set b.count =
        return flag;
    }

    private String localCount getCountById(Long id) {
        select a.count from a where a.id = id;
    }
}
